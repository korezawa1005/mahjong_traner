# frozen_string_literal: true

require "application_system_test_case"

class QuizFlowsTest < ApplicationSystemTestCase
  test "user solves a quiz" do
    skip "Implement once the quiz UI is testable via system tests"
  end
end
